apiVersion: v1
kind: Service
metadata:
    name: "base-e21e"
    labels:
        app: "base-e21e"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "base-e21e"